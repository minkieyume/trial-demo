use guix

export CL_SOURCE_REGISTRY="$(pwd)//:"

if has guix; then  
  PATHS=$(guix build wayland libxkbcommon mesa | grep -v "doc") 
  for p in $PATHS; do
    export LD_LIBRARY_PATH="$p/lib:$LD_LIBRARY_PATH"
  done
fi

# export LD_LIBRARY_PATH=$(guix build libxkbcommon | grep -v "doc")/lib:$LD_LIBRARY_PATH
# export LD_LIBRARY_PATH=$(guix build wayland | grep -v "doc")/lib:$LD_LIBRARY_PATH
# export LD_LIBRARY_PATH=$(guix build mesa)/lib:$LD_LIBRARY_PATH
